# Production Environment Configuration
# 프로덕션 환경 (실제 서비스 운영 서버)

# Server Configuration
server.port=8080

# Database Configuration (프로덕션 DB)
spring.datasource.url=jdbc:mysql://DFS-PRD-PUB-ALB-1550490073.ap-northeast-2.elb.amazonaws.com:3306/aqua?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=aqua
spring.datasource.password=aqua1234!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration (프로덕션 - 스키마 변경 금지)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Logging Configuration (프로덕션 - 최소 로그)
logging.level.com.dfs.aqua=WARN
logging.level.com.dfs.aqua.scheduler=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN
logging.level.org.springframework.orm.jpa=WARN
logging.level.org.springframework.transaction=WARN
logging.level.org.springframework.web=WARN

# JWT Configuration (프로덕션용 강력한 시크릿)
jwt.secret=dfs-aquarium-production-secret-key-2024-super-secure
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Scheduling Configuration
spring.task.scheduling.pool.size=2

# Error Handling (프로덕션 - 에러 정보 숨김)
server.error.include-stacktrace=never
server.error.include-message=never

# Production Security Settings
server.error.whitelabel.enabled=false